// Definir la matriz de vocales
let cadena = prompt("Escribe una cadena:");

// Recorrer la matriz y mostrar sus elementos de uno en uno
for (let i = 0; i < cadena.length; i++) {
	document.write(cadena[i]+"<br>");
}